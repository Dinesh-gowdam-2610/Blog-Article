const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: 'db4free.net',
    user: 'dinesh123',
    password: 'dinesh123',
    database: 'blobdetails',
  },
  listPerPage: 10,
};
module.exports = config;
